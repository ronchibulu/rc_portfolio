---
phase: 01-scaffold-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lockb
  - .gitignore
  - astro.config.mjs
  - tsconfig.json
  - biome.json
  - .prettierrc
  - .prettierignore
  - README.md
autonomous: true
requirements:
  - FND-001
tags:
  - astro
  - tooling
  - bun
  - biome
  - typescript

must_haves:
  truths:
    - "Running `bun install` after cloning produces the same dependency tree for every contributor (bun.lockb is committed)."
    - "`bun run dev` starts the Astro dev server and serves a page on localhost."
    - "`bun run build` produces a static `dist/` directory with no errors."
    - "`bun run check` runs Biome + `astro check` and exits 0 on the scaffolded project."
    - "`bun run format` formats .ts/.tsx/.js/.json with Biome and .astro with Prettier without either tool touching the other's files."
    - "`tsconfig.json` resolves `@/components/foo` to `./src/components/foo` (alias works in IDE + build)."
  artifacts:
    - path: "package.json"
      provides: "Bun-driven scripts, locked dependency versions for Astro v5, React 19, Vercel adapter, Biome, Prettier, astro-plugin, fontsource, three, r3f, drei, gsap, @gsap/react, motion, tailwindcss, @tailwindcss/vite, nanostores, detect-gpu. Wire lint/format/check/dev/build/preview scripts."
      contains: "\"dev\": \"astro dev\""
    - path: "bun.lockb"
      provides: "Bun binary lockfile — reproducible installs"
    - path: "astro.config.mjs"
      provides: "output:'static', @astrojs/vercel adapter, @astrojs/react integration, @tailwindcss/vite plugin via vite.plugins"
      contains: "output: 'static'"
    - path: "tsconfig.json"
      provides: "Astro's \"strict\" preset + baseUrl '.' + paths '@/*' → './src/*'"
      contains: "\"@/*\": [\"./src/*\"]"
    - path: "biome.json"
      provides: "TypeScript, React, a11y, and correctness rule sets enabled; includes src/**/*.{ts,tsx,js,jsx,json}; EXCLUDES *.astro"
      contains: "\"recommended\": true"
    - path: ".prettierrc"
      provides: "Prettier config enabling prettier-plugin-astro"
      contains: "prettier-plugin-astro"
    - path: ".prettierignore"
      provides: "Excludes everything that is NOT *.astro (so Prettier only formats .astro files)"
    - path: ".gitignore"
      provides: "Excludes node_modules/, dist/, .astro/, .vercel/, .DS_Store"
      contains: "node_modules"
    - path: "README.md"
      provides: "Setup instructions referencing bun, bun.lockb commit policy (D-03), explicit no-arbitrary-Tailwind-values rule (D-21, FND-009), dark-only note (D-22)"
      contains: "bun install"
  key_links:
    - from: "tsconfig.json paths"
      to: "src/*"
      via: "TypeScript path alias"
      pattern: "\"@/\\*\": \\[\"\\./src/\\*\"\\]"
    - from: "package.json scripts"
      to: "astro + biome + prettier + astro check"
      via: "bun run <script>"
      pattern: "\"lint\":|\"format\":|\"check\":"
    - from: "astro.config.mjs"
      to: "@astrojs/vercel + @astrojs/react + @tailwindcss/vite"
      via: "integrations + vite.plugins"
      pattern: "integrations: \\["
---

<objective>
Scaffold the Astro v5 static project skeleton at the repo root — package manager, build tooling, TypeScript, linting, formatting — so Plans 02 and 03 have a working project to configure.

Purpose: Establish the tool-chain and dependency baseline. This plan does NOT produce visible UI; it produces a buildable, lintable, type-checkable empty Astro project with all downstream dependencies pre-installed.

Output: A repo root containing `package.json`, `bun.lockb`, `astro.config.mjs`, `tsconfig.json`, `biome.json`, `.prettierrc`, `.prettierignore`, `.gitignore`, `README.md`, plus the baseline `src/` tree Astro generates (`src/env.d.ts`, possibly a default `src/pages/index.astro` that Plan 02 overwrites).
</objective>

<execution_context>
@C:/coding_proj/self_portfolio/.kilo/get-shit-done/workflows/execute-plan.md
@C:/coding_proj/self_portfolio/.kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-design-system/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<canonical_refs>
- `CLAUDE.md` — locked stack table (Astro v5, React 19, `@tailwindcss/vite`, Vercel adapter)
- `.planning/phases/01-scaffold-design-system/01-CONTEXT.md` — **D-01 through D-09, D-25** (bun, Biome, Prettier, TypeScript strict, `@/*` alias, astro.config)
- `.planning/research/PITFALLS.md §7, §8` — Tailwind v4 + Shadcn + Astro path alias setup order (this plan only does the tsconfig step; Plan 03 does the `shadcn init` step)
- `.planning/research/STACK.md §Recommended Versions` — version pins
</canonical_refs>

<planner_notes>
**API reconciliation for tailwind integration (CONTEXT.md D-15 vs current reality):**
D-15 says "`applyBaseStyles: false` on the Tailwind Vite plugin configuration." This option belonged to the deprecated `@astrojs/tailwind` integration (Tailwind v3 era). With Tailwind v4 + `@tailwindcss/vite` (CLAUDE.md's locked choice, STACK.md §Tailwind v4 Key Changes), the plugin does not double-load base styles — there is no `applyBaseStyles` option. Solution: **use `@tailwindcss/vite` in `vite.plugins` and do NOT install `@astrojs/tailwind`**. The INTENT of D-15 (don't load Tailwind base styles twice) is satisfied automatically. Document this interpretation in the astro.config.mjs comment so future readers understand.
</planner_notes>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize Astro project and install all Phase 1+ dependencies via bun</name>
  <files>package.json, bun.lockb, .gitignore, src/env.d.ts, src/pages/index.astro (Astro default — may be overwritten in Plan 02)</files>
  <read_first>
    - `.planning/phases/01-scaffold-design-system/01-CONTEXT.md` — D-01 (use bun), D-02 (commit bun.lockb), D-03 (README explicit about bun)
    - `CLAUDE.md` — Stack table for locked package versions (astro v5, three ^0.176, @react-three/fiber ^9, @react-three/drei ^10, gsap ^3.12, motion ^12, react 19)
    - `.planning/research/STACK.md` §Recommended Versions (2025)
  </read_first>
  <action>
    Run the following command sequence in the repo root (C:\coding_proj\self_portfolio):

    1. **Create the Astro project in-place** using `bun` (do NOT cd into a subdirectory — the repo root IS the project root per CONTEXT.md `<code_context>`):
       ```bash
       bun create astro@latest . --template minimal --typescript strict --no-install --no-git
       ```
       Flags explanation:
       - `.` = scaffold into current directory
       - `--template minimal` = barebones starter (we add integrations manually to honor D-25)
       - `--typescript strict` = locks Astro's "strict" preset (D-08)
       - `--no-install` = we run `bun install` ourselves so lockfile is bun.lockb
       - `--no-git` = repo is already a git repo

       If the minimal template creates a default `src/pages/index.astro`, leave it — Plan 02 overwrites it.

    2. **Install runtime + dev dependencies** in ONE batched `bun add` call so bun.lockb resolves cleanly:
       ```bash
       bun add \
         astro@^5 \
         @astrojs/react@^4 \
         @astrojs/vercel \
         react@^19 react-dom@^19 \
         three@^0.176 @react-three/fiber@^9 @react-three/drei@^10 \
         gsap@^3.12 @gsap/react \
         motion@^12 \
         tailwindcss@^4 @tailwindcss/vite@^4 \
         @fontsource/press-start-2p \
         nanostores @nanostores/react \
         detect-gpu \
         clsx tailwind-merge class-variance-authority \
         lucide-react
       ```
       Notes:
       - `clsx` + `tailwind-merge` + `class-variance-authority` are Shadcn runtime deps (Plan 03 imports them via `src/lib/utils.ts`); pre-installing here avoids a Plan 03 interruption.
       - `lucide-react` is Shadcn's default icon pack; pre-install.
       - DO NOT install `@astrojs/tailwind` — it is the deprecated v3-era integration (see planner_notes in <context>).

    3. **Install dev dependencies:**
       ```bash
       bun add -D \
         @types/react@^19 @types/react-dom@^19 @types/three \
         @biomejs/biome@^1 \
         prettier prettier-plugin-astro \
         typescript
       ```

    4. **Verify `bun.lockb` exists** (D-02). If `package-lock.json` or `pnpm-lock.yaml` appeared, delete them.

    5. **Create/extend `.gitignore`** at repo root with Astro + bun + Vercel exclusions:
       ```
       # deps
       node_modules/

       # build output
       dist/
       .astro/
       .vercel/

       # env
       .env
       .env.local
       .env.*.local

       # bun
       .bun/

       # OS
       .DS_Store
       Thumbs.db

       # editor
       .vscode/*
       !.vscode/extensions.json
       .idea/
       ```

    6. Do NOT edit `astro.config.mjs`, `tsconfig.json` yet — Task 2 replaces them with the final configured versions.
  </action>
  <verify>
    <automated>test -f package.json &amp;&amp; test -f bun.lockb &amp;&amp; test -f .gitignore &amp;&amp; ! test -f package-lock.json &amp;&amp; ! test -f pnpm-lock.yaml &amp;&amp; node -e "const p=require('./package.json'); const d=Object.keys(p.dependencies||{}); const dd=Object.keys(p.devDependencies||{}); const required=['astro','@astrojs/react','@astrojs/vercel','react','react-dom','three','@react-three/fiber','@react-three/drei','gsap','@gsap/react','motion','tailwindcss','@tailwindcss/vite','@fontsource/press-start-2p','nanostores','@nanostores/react','detect-gpu','clsx','tailwind-merge','class-variance-authority','lucide-react']; const devRequired=['@biomejs/biome','prettier','prettier-plugin-astro','typescript','@types/react','@types/react-dom','@types/three']; const missing=[...required.filter(r=>!d.includes(r)),...devRequired.filter(r=>!dd.includes(r))]; if(missing.length){console.error('MISSING:',missing);process.exit(1)} else console.log('OK')"</automated>
  </verify>
  <done>
    - `package.json` lists every dependency in the required list (runtime + dev) at the expected major versions.
    - `bun.lockb` exists at repo root and is a binary file.
    - No `package-lock.json` or `pnpm-lock.yaml` present.
    - `.gitignore` excludes `node_modules/`, `dist/`, `.astro/`, `.vercel/`, `.env`, OS junk, editor junk.
    - `@astrojs/tailwind` is NOT a dependency (only `tailwindcss` + `@tailwindcss/vite`).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write astro.config.mjs, tsconfig.json, and README.md</name>
  <files>astro.config.mjs, tsconfig.json, README.md</files>
  <read_first>
    - Output of Task 1 — current `package.json`, current `tsconfig.json` (whatever Astro's minimal template generated)
    - `.planning/phases/01-scaffold-design-system/01-CONTEXT.md` — **D-08** (TypeScript strict), **D-09** (`@/*` alias), **D-25** (output:static + Vercel + React + Tailwind-vite), **D-03** (README wording)
    - `.planning/research/PITFALLS.md §8` — exact Shadcn + Astro path alias setup order (step 1 = tsconfig paths FIRST)
  </read_first>
  <action>
    Overwrite the three files with these exact contents:

    **astro.config.mjs** (replaces whatever `bun create astro` generated):
    ```js
    // @ts-check
    import { defineConfig } from 'astro/config';
    import react from '@astrojs/react';
    import vercel from '@astrojs/vercel/static';
    import tailwindcss from '@tailwindcss/vite';

    // Phase 1 — D-25: static output, Vercel adapter, React islands, Tailwind v4 via Vite plugin.
    // Note: D-15 references `applyBaseStyles: false` which was an option of the deprecated
    // @astrojs/tailwind integration. With @tailwindcss/vite (the v4 path) base styles are not
    // loaded twice, so the intent of D-15 is satisfied without an explicit flag.
    export default defineConfig({
      output: 'static',
      adapter: vercel(),
      integrations: [react()],
      vite: {
        plugins: [tailwindcss()],
      },
    });
    ```

    **tsconfig.json** (fully replace the Astro-generated one — preserve `extends` only):
    ```json
    {
      "extends": "astro/tsconfigs/strict",
      "compilerOptions": {
        "baseUrl": ".",
        "paths": {
          "@/*": ["./src/*"]
        },
        "jsx": "react-jsx",
        "jsxImportSource": "react"
      },
      "include": [".astro/types.d.ts", "**/*"],
      "exclude": ["dist", "node_modules"]
    }
    ```
    Rationale:
    - `extends: "astro/tsconfigs/strict"` honors D-08 (strict not strictest).
    - `baseUrl` + `paths` honors D-09 and PITFALLS §8 step 1 — this MUST exist BEFORE Plan 03 runs `shadcn init`.
    - `jsx: "react-jsx"` + `jsxImportSource: "react"` keeps .tsx files compiling against React 19.

    **README.md** (single authoritative setup doc per D-03):
    ```markdown
    # Ronald Cheng — Portfolio

    Dark pixel/8-bit retro portfolio built on **Astro v5** static + **React 19** islands,
    with a future R3F gaming-setup scene driven by GSAP ScrollTrigger and an OS-style
    project browser.

    ## Requirements

    - **Bun** — this project uses bun as its sole package manager. Do **not** run
      `npm install` or `pnpm install`; both will create a competing lockfile that
      fights `bun.lockb`.
      Install bun: https://bun.sh/docs/installation

    ## Setup

    ```bash
    bun install
    bun run dev
    ```

    Dev server defaults to http://localhost:4321.

    ## Scripts

    | Script | What it does |
    |--------|--------------|
    | `bun run dev` | Astro dev server with HMR |
    | `bun run build` | Static production build to `dist/` |
    | `bun run preview` | Serve the built `dist/` locally |
    | `bun run lint` | Biome lint + `astro check` type-check |
    | `bun run format` | Biome format .ts/.tsx/.js/.json + Prettier on .astro |
    | `bun run check` | CI-safe: lint + type-check, fails on warnings |

    ## Project rules (enforced by review)

    - **Dark mode only.** No light/dark toggle. Baseline background is `zinc-950`.
    - **Default Tailwind scale only.** Do NOT use arbitrary values like `pt-[50px]`
      or `bg-[#ff00ff]`. First violation in PR review is rejected.
    - **Single package manager.** `bun.lockb` is committed; other lockfiles are removed.
    - **R3F Canvas uses `client:only="react"`** (added in Phase 4).
    - **GSAP via `useGSAP()` only** (added in Phase 6).

    See `CLAUDE.md` for the full locked stack table and hard rules.
    See `.planning/` for phase-by-phase planning artifacts.
    ```

    Do NOT modify `package.json` in this task (Task 3 adds scripts).
  </action>
  <verify>
    <automated>test -f astro.config.mjs &amp;&amp; test -f tsconfig.json &amp;&amp; test -f README.md &amp;&amp; grep -q "output: 'static'" astro.config.mjs &amp;&amp; grep -q "@tailwindcss/vite" astro.config.mjs &amp;&amp; grep -q "vercel" astro.config.mjs &amp;&amp; grep -q "react" astro.config.mjs &amp;&amp; node -e "const t=require('./tsconfig.json'); if(t.extends!=='astro/tsconfigs/strict')throw Error('extends wrong'); if(!t.compilerOptions.paths['@/*'][0].includes('src'))throw Error('alias wrong'); console.log('OK')" &amp;&amp; grep -q "bun install" README.md &amp;&amp; grep -q "arbitrary values" README.md</automated>
  </verify>
  <done>
    - `astro.config.mjs` sets `output: 'static'`, includes `vercel()` adapter, `react()` integration, `@tailwindcss/vite` in `vite.plugins`.
    - `astro.config.mjs` does NOT import or reference `@astrojs/tailwind`.
    - `tsconfig.json` extends `astro/tsconfigs/strict`, sets `baseUrl: "."`, maps `@/*` → `./src/*`, includes `jsx: "react-jsx"`.
    - `README.md` documents bun-only install, the six scripts, and the dark-only + no-arbitrary-values rules.
    - The file contents quoted in <action> are byte-identical on disk (or functionally equivalent if a code formatter runs).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Configure Biome + Prettier, wire bun scripts, and verify the scaffold builds clean</name>
  <files>biome.json, .prettierrc, .prettierignore, package.json (scripts block only)</files>
  <read_first>
    - `.planning/phases/01-scaffold-design-system/01-CONTEXT.md` — **D-04** (Biome scope), **D-05** (astro check handles .astro), **D-06** (Prettier scoped to .astro), **D-07** (three scripts)
    - Current `package.json` after Tasks 1–2 (do not blow away existing fields; add/merge the scripts block)
  </read_first>
  <action>
    1. **Create `biome.json`** at repo root (Biome v1 schema):
       ```json
       {
         "$schema": "https://biomejs.dev/schemas/1.9.4/schema.json",
         "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
         "files": {
           "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.js", "src/**/*.jsx", "src/**/*.json", "*.mjs", "*.json"],
           "ignore": ["**/*.astro", "dist/**", ".astro/**", "node_modules/**", "bun.lockb"]
         },
         "organizeImports": { "enabled": true },
         "linter": {
           "enabled": true,
           "rules": {
             "recommended": true,
             "a11y": { "recommended": true },
             "correctness": { "recommended": true },
             "suspicious": { "recommended": true },
             "style": { "recommended": true },
             "complexity": { "recommended": true }
           }
         },
         "formatter": {
           "enabled": true,
           "indentStyle": "space",
           "indentWidth": 2,
           "lineWidth": 100,
           "lineEnding": "lf"
         },
         "javascript": {
           "formatter": {
             "quoteStyle": "single",
             "jsxQuoteStyle": "double",
             "semicolons": "always",
             "trailingCommas": "all"
           }
         }
       }
       ```
       Rationale: D-04 requires TS/React/a11y/correctness rule sets. `"**/*.astro"` in `ignore` prevents Biome from touching Astro files (D-06 gives those to Prettier). `"lineEnding": "lf"` keeps files consistent on Windows (CLAUDE.md & CONTEXT.md `<code_context>` note CRLF warnings).

    2. **Create `.prettierrc`** at repo root — Prettier config that ONLY affects .astro:
       ```json
       {
         "plugins": ["prettier-plugin-astro"],
         "singleQuote": true,
         "semi": true,
         "printWidth": 100,
         "tabWidth": 2,
         "useTabs": false,
         "overrides": [
           {
             "files": "*.astro",
             "options": {
               "parser": "astro"
             }
           }
         ]
       }
       ```

    3. **Create `.prettierignore`** at repo root — exclude EVERYTHING Prettier should not touch (D-06 scope-tight):
       ```
       # Prettier only formats *.astro in this repo.
       # Biome owns .ts/.tsx/.js/.jsx/.json. Everything below is intentionally ignored.
       *
       !*.astro
       !src/
       src/**/*
       !src/**/*.astro
       dist/
       .astro/
       node_modules/
       bun.lockb
       ```
       The `*` then `!*.astro` + `!src/` + re-exclusion pattern scopes Prettier to `src/**/*.astro` only.

    4. **Merge scripts into `package.json`** (D-07 + standard Astro scripts). Open `package.json`, replace its `"scripts"` key with:
       ```json
       "scripts": {
         "dev": "astro dev",
         "start": "astro dev",
         "build": "astro check &amp;&amp; astro build",
         "preview": "astro preview",
         "astro": "astro",
         "lint": "biome lint src &amp;&amp; astro check",
         "format": "biome format --write src *.mjs *.json &amp;&amp; prettier --write \"src/**/*.astro\"",
         "check": "biome check src &amp;&amp; astro check"
       }
       ```
       Notes:
       - `build` runs `astro check` BEFORE `astro build` so type errors fail the build (not surface in prod).
       - `lint` = Biome + astro check (D-07).
       - `format` = Biome (write) on owned files + Prettier on .astro only (D-06).
       - `check` = CI-safe; `biome check` runs lint + format-check without writing.
       - Preserve ALL other fields of `package.json` (name, version, type, dependencies, devDependencies) — only replace the scripts object.

    5. **Ensure `package.json` has `"type": "module"`** (required for `astro.config.mjs` ESM and for React 19). If missing, add it.

    6. **Run the full tool-chain to verify nothing is broken:**
       ```bash
       bun run check
       bun run build
       ```
       `bun run check` should pass with zero errors. `bun run build` should produce a `dist/` directory with a minimal index.html (whatever Astro's default page looks like; Plan 02 replaces it). If either command fails, fix the root cause before marking the task done — do NOT skip verify.
  </action>
  <verify>
    <automated>test -f biome.json &amp;&amp; test -f .prettierrc &amp;&amp; test -f .prettierignore &amp;&amp; node -e "const p=require('./package.json'); const s=p.scripts||{}; ['dev','build','preview','lint','format','check'].forEach(k=>{if(!s[k])throw Error('missing script: '+k)}); if(p.type!=='module')throw Error('package.json must be type:module'); console.log('scripts OK')" &amp;&amp; bun run check &amp;&amp; bun run build &amp;&amp; test -d dist</automated>
  </verify>
  <done>
    - `biome.json` contains `linter.enabled: true` with recommended + a11y + correctness rule sets, `files.ignore` includes `**/*.astro`, `lineEnding: "lf"`.
    - `.prettierrc` enables `prettier-plugin-astro`.
    - `.prettierignore` excludes everything except `src/**/*.astro`.
    - `package.json` `"type": "module"` is set; `scripts` block has all 6 of: dev, build, preview, lint, format, check.
    - `bun run check` exits 0.
    - `bun run build` exits 0 and creates `dist/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in Phase 1 scope) | This plan produces a static scaffold with no user input, no data persistence, no authentication, no network requests beyond the static asset fetch performed by the browser. All code runs at build time or renders static HTML/CSS/JS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | `package.json` dependencies | mitigate | Commit `bun.lockb` (D-02) so dependency versions are reproducible; reject stray `package-lock.json` / `pnpm-lock.yaml` that could resolve to different versions (D-02). |
| T-01-02 | E (Elevation of Privilege) | `postinstall` / build scripts in transitive deps | accept | Dependencies are well-known OSS (Astro, React, Tailwind, Biome, Prettier, Three.js, GSAP, Motion). Supply-chain review is out of scope for a personal portfolio; user accepts standard npm registry risk. `bun.lockb` pins versions to prevent drift. |
| T-01-03 | I (Information Disclosure) | `.env` or similar | mitigate | `.gitignore` excludes `.env`, `.env.local`, `.env.*.local` so any future secrets do not leak. Phase 1 introduces zero secrets. |
| T-01-04 | D (Denial of Service) | Dev server on localhost | accept | Dev server binds localhost; not exposed. Production is static Vercel deploy — no server-side code paths to DoS. |
</threat_model>

<verification>
Phase-level checks after all three tasks complete:

1. `bun install` works on a fresh clone.
2. `bun run dev` starts without TypeScript / Astro / Vite errors.
3. `bun run check` passes (Biome + astro check).
4. `bun run build` produces `dist/` successfully.
5. `tsconfig.json` `@/*` alias resolves (verify with a quick `bun run astro check` against a file using the alias — in this plan there's no such file, so verification is satisfied by the config itself + preservation in Plan 02).
6. No competing lockfile (`package-lock.json`, `pnpm-lock.yaml`) present.
</verification>

<success_criteria>
- All three tasks pass their `<verify>` blocks.
- All nine files in `files_modified` exist at repo root.
- `bun run build` exits 0; `bun run check` exits 0.
- No dependency on Tailwind, Shadcn, or font wiring completed yet — those belong to Plans 02 and 03. This plan is deliberately tooling-only.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-design-system/01-01-SUMMARY.md` documenting:
- Exact bun and astro versions installed (`bun --version`, `bun run astro --version`)
- Any deviations from the locked dependency list (shouldn't be any — document why if so)
- Confirmation that `bun run build` produced `dist/` cleanly
- Any transient errors hit during setup and how they were resolved
</output>
