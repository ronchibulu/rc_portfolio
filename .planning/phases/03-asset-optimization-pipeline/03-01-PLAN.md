---
phase: 03-asset-optimization-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/models/gaming_setup_v12.glb
  - public/draco/draco_decoder.js
  - public/draco/draco_decoder.wasm
  - public/draco/draco_encoder.js
  - public/draco/draco_wasm_wrapper.js
  - package.json
autonomous: true
requirements:
  - SCENE-001
  - SCENE-002
  - PERF-004

tags:
  - gltf-transform
  - draco
  - asset-optimization
  - 3d

must_haves:
  truths:
    - "`public/models/gaming_setup_v12.glb` exists and is under 2,097,152 bytes (2 MB)."
    - "`public/draco/` contains draco_decoder.js, draco_decoder.wasm, draco_encoder.js, draco_wasm_wrapper.js."
    - "Optimized .glb has `KHR_draco_mesh_compression` in extensionsRequired (confirmed via gltf-transform inspect)."
    - "Vertex count within 1% of source model (geometry integrity preserved)."
    - "`package.json` contains `optimize-model` script for reproducible re-optimization."
  artifacts:
    - path: "public/models/gaming_setup_v12.glb"
      provides: "Draco-compressed gaming setup model for Phase 5 R3F scene (SCENE-001)"
      contains: "KHR_draco_mesh_compression"
    - path: "public/draco/draco_decoder.wasm"
      provides: "WebAssembly Draco decoder for three.js DRACOLoader (SCENE-002)"
    - path: "package.json"
      provides: "optimize-model script: dedup+prune+flatten+weld+draco pipeline"
      contains: "optimize-model"
  key_links:
    - from: "public/models/gaming_setup_v12.glb"
      to: "useGLTF('/models/gaming_setup_v12.glb') in Phase 5"
      via: "Draco-compressed glTF 2.0"
      pattern: "KHR_draco_mesh_compression"
    - from: "public/draco/"
      to: "useGLTF.setDecoderPath('/draco/') in Phase 5"
      via: "three.js DRACOLoader"
      pattern: "draco_decoder"
---

<objective>
Optimize gaming_setup_v12.glb (51 MB source) to under 2 MB using gltf-transform with Draco geometry compression. Set up self-hosted Draco decoder binaries in public/draco/ for Phase 5 R3F integration. Add reproducible optimization script to package.json.
</objective>

## Pipeline

1. `gltf-transform dedup` — remove duplicate accessors (–95 accessors removed)
2. `gltf-transform prune` — remove unused nodes/materials (–21 nodes removed, 40.26 MB)
3. `gltf-transform flatten` — flatten node hierarchy (40.26 MB)
4. `gltf-transform weld` — merge duplicate vertices (39.48 MB)
5. `gltf-transform draco --quantize-position 10 --quantize-normal 8 --quantize-texcoord 10` — **1.89 MB**

## Results

| Step | Size | Reduction |
|------|------|-----------|
| Source | 51.55 MB | — |
| After dedup | 51.51 MB | <0.1% |
| After prune | 40.26 MB | 22% |
| After flatten+weld | 39.48 MB | 23.4% |
| After Draco (pos:10) | **1.89 MB** | **96.3%** |

Target: < 2 MB (**PASS** — 1.89 MB = 1,888,296 bytes < 2,097,152 bytes)
